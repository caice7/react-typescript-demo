import React from 'react';
import ReactDOM from 'react-dom';
import './index.scss';
import APP from './app';

ReactDOM.render(
  <React.StrictMode>
    <APP />
  </React.StrictMode>,
  document.getElementById('root')
);
